generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            String         @id @default(cuid())
  email         String         @unique
  password      String
  name          String?
  role          Role           @default(USER)
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
  phone         String?
  twoFactorSecret String?
  twoFactorEnabled Boolean      @default(false)
  classBookings ClassBooking[]
  progress      Progress[]
  purchases     Purchase[]
  reviews       Review[]
  certificates  Certificate[]
}

model Admin {
  id               String   @id @default("admin")
  email            String   @unique
  password         String
  twoFactorSecret  String?
  twoFactorEnabled Boolean  @default(false)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}

model Course {
  id            String        @id @default(cuid())
  title         String
  subtitle      String?
  description   String
  price         Int
  originalPrice Int?
  duration      String
  lessons       Int
  level         String
  language      String        @default("Hindi/English")
  thumbnail     String?
  instructor    String        @default("Kashmira Chakraborty")
  rating        Float         @default(4.8)
  students      Int           @default(0)
  featured      Boolean       @default(false)
  published     Boolean       @default(true)
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  whatYouLearn  String[]
  requirements  String[]
  trailerUrl    String?
  popular       Boolean       @default(false)
  progress      Progress[]
  purchases     Purchase[]
  curriculum    Section[]
  reviews       Review[]
  certificates  Certificate[]
}

model Section {
  id       String  @id @default(cuid())
  title    String
  lessons  Int
  duration String
  order    Int
  courseId String
  course   Course  @relation(fields: [courseId], references: [id], onDelete: Cascade)
  videos   Video[]
}

model Video {
  id          String  @id @default(cuid())
  title       String
  description String?
  youtubeUrl  String
  duration    String
  order       Int
  isPreview   Boolean @default(false)
  sectionId   String
  section     Section @relation(fields: [sectionId], references: [id], onDelete: Cascade)
}

model Purchase {
  id              String   @id @default(cuid())
  userId          String
  courseId        String
  amount          Int
  status          String   @default("pending")
  razorpayOrderId String?
  razorpayPaymentId String?
  razorpaySignature String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt @default(now())
  course          Course   @relation(fields: [courseId], references: [id])
  user            User     @relation(fields: [userId], references: [id])

  @@unique([userId, courseId])
}

model Progress {
  id        String   @id @default(cuid())
  userId    String
  courseId  String
  videoId   String
  timestamp Int      @default(0)
  completed Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User     @relation(fields: [userId], references: [id])
  course    Course   @relation(fields: [courseId], references: [id])

  @@unique([userId, courseId, videoId])
  @@map("video_progress")
}



model SiteContent {
  id        String   @id @default(cuid())
  key       String   @unique
  value     String
  type      String   @default("text")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ClassBooking {
  id        String   @id @default(cuid())
  userId    String
  classType String
  phone     String
  status    String   @default("PENDING")
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id])
}

model Review {
  id        String   @id @default(cuid())
  userId    String
  courseId  String
  rating    Int      @db.SmallInt
  comment   String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User     @relation(fields: [userId], references: [id])
  course    Course   @relation(fields: [courseId], references: [id])

  @@unique([userId, courseId])
}

model Certificate {
  id           String   @id @default(cuid())
  userId       String
  courseId     String
  certificateId String  @unique
  issuedAt     DateTime @default(now())
  user         User     @relation(fields: [userId], references: [id])
  course       Course   @relation(fields: [courseId], references: [id])

  @@unique([userId, courseId])
}

model CertificateSettings {
  id                   String @id @default(cuid())
  templateUrl          String @default("/certificates/template.png")
  studentNameX         Int    @default(562)
  studentNameY         Int    @default(400)
  studentNameWidth     Int    @default(200)
  studentNameHeight    Int    @default(40)
  courseTitleX         Int    @default(562)
  courseTitleY         Int    @default(500)
  courseTitleWidth     Int    @default(250)
  courseTitleHeight    Int    @default(40)
  dateX                Int    @default(150)
  dateY                Int    @default(700)
  dateWidth            Int    @default(150)
  dateHeight           Int    @default(30)
  certificateIdX       Int    @default(973)
  certificateIdY       Int    @default(700)
  certificateIdWidth   Int    @default(120)
  certificateIdHeight  Int    @default(30)
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt
}

enum Role {
  USER
  ADMIN
}
